# Phase 08.1: Claude Code 辅助请求隔离与高级工具兼容 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-13
**Phase:** 08.1-Claude Code 辅助请求隔离与高级工具兼容
**Areas discussed:** 标题辅助请求旁路、精确路径召回策略、ToolSearch 422 调查门禁

---

## 标题辅助请求旁路

| Option | Description | Selected |
|--------|-------------|----------|
| 完全安全直通 | 跳过所有有状态记忆管线，只保留安全 debug、转发与 usage | ✓ |
| 只跳过 Archive | 仍允许 Frozen、压缩等处理 | |
| 只禁止状态写入 | 允许读取状态但不更新 | |

**User's choice:** 完全安全直通。
**Notes:** 双层识别；本阶段只覆盖自动会话标题；记录一条不含正文的 Info 审计日志。

### 缺少 output_config 时的识别

| Option | Description | Selected |
|--------|-------------|----------|
| 双层识别 | Schema 强命中；无 Schema 时要求单 user + `<session>` + 标题 system | ✓ |
| 只认 Schema | 缺 Schema 按 main 处理 | |
| 宽松识别 | `<session>` + 标题 system 即命中 | |

### 辅助请求覆盖范围

| Option | Description | Selected |
|--------|-------------|----------|
| 只处理自动会话标题 | 等真实抓包后再扩展其他 auxiliary | ✓ |
| 覆盖标题同族 | 同时处理 rename 与 teleport | |
| 全部 auxiliary | 尝试统一识别所有后台请求 | |

---

## 精确路径召回策略

| Option | Description | Selected |
|--------|-------------|----------|
| 完整自动召回 | 路径精确匹配即可完整展开 | |
| 只注入摘要 | 路径精确匹配后注入有限摘要 | |
| 不独立触发 | 只有恢复意图或 deep_search 才召回 | ✓ |

**User's choice:** 精确路径在正常编码对话中普遍存在，不应独立触发 Archive。
**Notes:** 目标同时减少无用 token 消耗和不一定相关的历史污染。

---

## ToolSearch 422 调查门禁

| Option | Description | Selected |
|--------|-------------|----------|
| 直接规划上游降级 | 假定上游不支持 ToolSearch 并转换协议 | |
| Investigation-only | 控制变量确认根因后才重新规划修复 | ✓ |
| 完全排除 | 不在 Phase 08.1 追踪 | |

**User's choice:** 根因没有定论，先让独立 AI 复核；两个复核均建议只作为调查项。
**Notes:** `data/debug/B` 是随意请求产生的第二次自然复现，不是受控 A/B。它把引用数量降到 2 仍返回 422，排除数量假设，但没有隔离 sawtooth 消息拆分。

## the agent's Discretion

- 分类纯函数、reason 枚举、测试 helper 与脱敏结构差分输出的具体实现。
- 召回信号类型是删除还是保留但不生成，只要外部行为符合已锁定决策。

## Deferred Ideas

- 其他 auxiliary 请求分类。
- 根因确认前的 ToolSearch 降级、provider capability、自动重试与 live 重放。
