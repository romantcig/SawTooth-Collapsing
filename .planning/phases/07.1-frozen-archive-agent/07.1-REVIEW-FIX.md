---
phase: 07.1-frozen-archive-agent
fixed_at: 2026-07-11T14:45:13Z
review_path: .planning/phases/07.1-frozen-archive-agent/07.1-REVIEW.md
iteration: 2
findings_in_scope: 3
fixed: 3
skipped: 0
status: all_fixed
---

# Phase 07.1: Code Review Fix Report

**Fixed at:** 2026-07-11T14:02:46Z
**Source review:** `.planning/phases/07.1-frozen-archive-agent/07.1-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 11
- Fixed: 11
- Skipped: 0
- `ADJ-01` 未修改：无 thinking 的 Claude 请求旁路 Sawtooth 是已接受的产品契约。

## Fixed Issues

### CR-01: Debug 请求快照会把 Anthropic API key 明文写入磁盘
**Files modified:** `internal/proxy/forward.go`, `internal/proxy/forward_test.go`
**Commit:** c719531
**Applied fix:** 对常见认证与会话凭证 header 做大小写不敏感脱敏，并增加凭证不落盘测试。

### CR-02: 未校验的 session header 可穿越 debug 根目录
**Files modified:** `internal/proxy/forward.go`, `internal/proxy/forward_test.go`
**Commit:** 582d98c
**Applied fix:** 目录只使用 session ID 的 SHA-256，并以绝对路径和 `filepath.Rel` 验证根目录约束。

### CR-03: Frozen boundary hash 忽略大部分消息内容
**Files modified:** `internal/proxy/frozen.go`, `internal/proxy/frozen_test.go`, `internal/proxy/proxy.go`, `internal/proxy/proxy_pipeline_test.go`
**Commits:** 7e663bb, 9799782
**Applied fix:** 对 StripReminders 后完整 boundary 的规范 JSON 计算 SHA-256，并深拷贝原始 boundary，覆盖长文本后缀、第二文本块和 tool_result 修改。
**Status:** fixed: requires human verification

### CR-05: 损坏的持久化 Frozen cutoff 可触发 panic
**Files modified:** `internal/proxy/frozen.go`, `internal/proxy/frozen_test.go`, `internal/proxy/proxy.go`
**Commit:** 2248b4d
**Applied fix:** Store、冷启动加载、Get 和切片前验证 cutoff、boundary hash 与 token 元数据，非法状态 fail closed。
**Status:** fixed: requires human verification

### WR-01: cachedTTL 并发读写 data race
**Files modified:** `internal/proxy/proxy.go`, `internal/proxy/cache_test.go`
**Commit:** 165c6c8
**Applied fix:** 用专用 mutex 原子保护 TTL 比较、setter 与状态更新。
**Status:** fixed: requires human verification

### WR-02: 同一 session 并发持久化可写回旧 Frozen 快照
**Files modified:** `internal/proxy/frozen.go`, `internal/proxy/frozen_test.go`
**Commit:** 7725393
**Applied fix:** 将 Frozen 内存更新与 persist callback 串行化，增加可控阻塞并发测试。
**Status:** fixed: requires human verification

### WR-03: Debug 文件名毫秒级碰撞
**Files modified:** `internal/proxy/forward.go`, `internal/proxy/forward_test.go`
**Commit:** 7d403de
**Applied fix:** 使用纳秒时间戳与 request ID，并以 `O_CREATE|O_EXCL`、0600 权限创建文件。

### WR-04: WAL 数据库逐文件复制无法证明快照一致
**Files modified:** `internal/proxy/archive_readonly_audit_test.go`
**Commit:** 93ccbf6
**Applied fix:** 复制窗口前后重复校验 DB/WAL/SHM 指纹，不稳定时跳过并提示停止 writer。
**Status:** fixed: requires human verification

### WR-05: CollapseOldMessages 输入长度不一致会 panic
**Files modified:** `internal/proxy/collapse.go`, `internal/proxy/collapse_test.go`
**Commit:** efcfb82
**Applied fix:** 切片前验证输入等长、同步应用上限并双侧校验 cutoff，异常输入原样 fail closed。
**Status:** fixed: requires human verification

### WR-06: Agent 与 Frozen 日志缺少 request_id
**Files modified:** `internal/proxy/session.go`, `internal/proxy/session_test.go`, `internal/proxy/proxy.go`, `internal/proxy/frozen.go`, `internal/proxy/proxy_pipeline_test.go`
**Commit:** 6d850f1
**Applied fix:** Agent 与 Frozen 关键事件统一使用 request-scoped logger，并验证并发链路携带 request ID。
**Status:** fixed: requires human verification

### WR-07: Archive hash 非 canonical 且信任调用方 hash
**Files modified:** `internal/proxy/collapse.go`, `internal/proxy/collapse_test.go`, `internal/proxy/store.go`, `internal/proxy/store_test.go`
**Commit:** 79c1a93
**Applied fix:** 解析 RawMessage 后稳定编码；SaveArchive 始终从实际正文重算 hash，覆盖键顺序、空白和伪造 hash 测试。
**Status:** fixed: requires human verification

### Supplemental: SQLite 临时目录清理竞态
**Files modified:** `internal/proxy/store.go`, `internal/proxy/store_test.go`
**Commit:** ef28771
**Applied fix:** `SQLiteStore` 保存数据库路径，在 checkpoint 与连接关闭完成后主动清理该数据库专属的 WAL/SHM 文件，避免 Windows `t.TempDir()` 清理期间伴生文件再次出现；增加 Close 后伴生文件不存在的回归测试。
**Status:** fixed: requires human verification

## Iteration 2 Fixed Issues

### CR-01: Close 在 checkpoint 未完成时删除 WAL
**Files modified:** `internal/proxy/store.go`, `internal/proxy/store_test.go`
**Commit:** 6d47adf
**Applied fix:** `Close` 扫描并验证 `wal_checkpoint(TRUNCATE)` 返回的 busy/log/checkpointed；checkpoint 报错、busy 或未完全回写时关闭连接但保留 WAL/SHM，并聚合返回 checkpoint/close 错误。只有 checkpoint 明确完成且关闭成功时才执行伴生文件清理。增加真实活跃 reader busy 场景，验证 WAL 保留且已提交状态重新打开后仍存在；增加 checkpoint error 路径伴生文件保留测试。
**Status:** fixed: requires human verification

### WR-01: Debug 写入失败遗留截断文件
**Files modified:** `internal/proxy/forward.go`, `internal/proxy/forward_test.go`
**Commit:** ba72f8c
**Applied fix:** 抽出可注入的 debug 文件写入 helper，检查短写、Write error 与 Close error；创建成功后的任一失败都会关闭并删除最终路径，同时由原调用点保留错误日志。覆盖写失败和关闭失败两条回归。

### WR-02: Frozen 失效未删除持久化坏状态
**Files modified:** `internal/proxy/frozen.go`, `internal/proxy/frozen_test.go`, `internal/proxy/store.go`, `internal/proxy/store_test.go`, `cmd/proxy/main.go`
**Commit:** bd125e5
**Applied fix:** 为 Frozen persistence abstraction 增加删除回调，SQLiteStore 增加 `DeleteState` 并在生产装配；验证失败与冷启动损坏状态会删除 `frozen:<threadID>`，同时保留当前进程已加载标记，防止删除失败时重复恢复。覆盖 boundary 失效后持久层键消失且第二次 Get 不再调用 load。
**Status:** fixed: requires human verification

## Verification

- 每项 targeted `go test`：PASS。
- `TestSearchArchivesStableOrderingAndFields` 与 `TestSQLiteStoreCloseRemovesWALCompanions` 联合执行 `-count=50`：PASS。
- `go test ./internal/proxy -count=1`：PASS。
- `go vet ./internal/proxy`：PASS。
- Iteration 2 targeted tests（checkpoint busy/error、debug write/close failure、Frozen delete/reload、`cmd/proxy` 编译）：PASS。
- 所有 Go 修改均已执行 `gofmt`。
- 未执行 `go test -race`；WR-01/WR-02 建议在可用 MinGW race toolchain 的 Windows 环境复核。

---

_Fixed: 2026-07-11T14:45:13Z_
_Fixer: the agent (gsd-code-fixer, generic-agent workaround)_
_Iteration: 2_
