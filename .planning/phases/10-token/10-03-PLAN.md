---
phase: 10-token
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/proxy/session.go
  - internal/proxy/session_test.go
  - internal/proxy/request_meta.go
  - internal/proxy/phase08_integration_test.go
autonomous: true
requirements:
  - PRESSURE-04
must_haves:
  truths:
    - "D-15/D-16：2.1.207 独立 system attribution text block 只有同时具备 `x-anthropic-billing-header:` 前缀与严格 token 边界 `cc_is_subagent=true` 才分类为可靠 subagent"
    - "D-10/D-11：可靠 subagent 身份写入 requestMeta 并贯穿请求入口到 JSON/SSE response usage，统一 predicate 禁止主 pressure 状态读写"
    - "D-12/D-17：保留 HTTP billing header 与结构化 agentContext 强入口；未命中或畸形输入沿用 main，不引入冲突框架或第三种状态模式"
    - "D-23/D-24：subagent 可保留受限 response_usage facts，但 logger/facts 不携带 attribution 正文、billing header、parent/session ID，也不输出主压力摘要"
  artifacts:
    - path: "internal/proxy/session.go"
      provides: "2.1.207 system attribution block 强特征与受限分类 reason"
    - path: "internal/proxy/request_meta.go"
      provides: "request-scoped agent role 与单一 Sawtooth state predicate"
    - path: "internal/proxy/session_test.go"
      provides: "system attribution 强/弱形态矩阵"
    - path: "internal/proxy/phase08_integration_test.go"
      provides: "billing、agentContext、system attribution、unknown-is-main 隔离矩阵"
  key_links:
    - from: "inspectAgentSystem"
      to: "classifyAgentFeatures"
      via: "独立 attribution block 产生布尔强特征和 `system_attribution` 受限 reason"
      pattern: "inspectAgentSystem|system_attribution"
    - from: "agentClassification.Role"
      to: "requestMeta.tracksSawtoothState"
      via: "入口将 role 写入 meta，title/subagent 共享唯一 false predicate"
      pattern: "AgentRole|tracksSawtoothState"
---

<objective>
补齐 Claude Code 2.1.207 原生 system attribution subagent 强特征，并把代理角色固化为请求级状态写回权限。

Purpose: 避免可靠 subagent 在入口旁路后仍由响应路径按普通主请求写回 actual baseline，同时避免全文泛搜造成主请求误旁路。
Output: attribution block 窄分类、requestMeta role contract 与主/子代理隔离矩阵。
</objective>

<execution_context>
@C:/Users/romantcig/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/romantcig/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-token/10-CONTEXT.md
@.planning/phases/10-token/10-RESEARCH.md
@.planning/phases/10-token/10-PATTERNS.md
@.planning/phases/08-claude-code-token-user-context-usage-debug/08-CONTEXT.md

<interfaces>
- 2.1.207 attribution 由独立 system text block 承载，形如 `x-anthropic-billing-header: cc_version=...; cc_entrypoint=...; ... cc_is_subagent=true; ...`。
- `billingSubagentMarkerPattern` 的 token boundary 语义应复用；不得在所有 system 文本中搜索裸 `subagent` 或裸 `cc_is_subagent`。
- `requestMeta.tracksSawtoothState()` 必须保持 nil/零值默认 true；仅 `RequestKind=session_title` 或 `AgentRole=subagent` 返回 false，unknown/main 继续 true。
- `auxiliaryLogger` 继续只继承 request_id，不继承 request_session_id。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 识别独立 2.1.207 system attribution subagent block</name>
  <read_first>
    - internal/proxy/session.go
    - internal/proxy/session_test.go
    - .planning/phases/10-token/10-CONTEXT.md
    - .planning/phases/10-token/10-RESEARCH.md
    - .planning/phases/10-token/10-PATTERNS.md
  </read_first>
  <files>internal/proxy/session.go, internal/proxy/session_test.go</files>
  <behavior>
    - D-15：独立 text block 以 `x-anthropic-billing-header:` 开头且 header payload 含严格边界 `cc_is_subagent=true` 时返回 subagent。
    - D-15：HTTP billing header、`agentContext.agentType=subagent`、非空 parentSessionId 继续按原优先级工作。
    - D-16：普通 system 正文中的单词、相似 key、`false`、缺前缀、非 text block、跨 block 拼接均不得分类为 subagent。
    - D-12/D-17：未匹配与畸形请求返回 main/no_subagent_marker，不创建 unknown/conflict/第三种状态。
  </behavior>
  <action>先在 `session_test.go` 新增 `TestAgentSystemAttributionStrongFeature` RED 表，再扩展 `agentRequestFeatures` 增加 `SystemAttributionSubagent bool`，扩展 `agentClassificationReason` 增加 `agentReasonSystemAttribution`。在 `inspectAgentSystem` 遍历单个 system array item 时，仅对 `type` 缺失或等于 `text` 且 `text` 去首尾空白后以固定 attribution 前缀开头的独立 block 解析 marker；marker 使用现有 token-boundary regex 检查 payload。不要跨 block 拼接，不要搜索任意正文，不要保留 header 文本。</action>
  <verify>
    <automated>go test ./internal/proxy -run 'TestAgent(SystemAttributionStrongFeature|BillingMarker|ContextStrongFeatures|ClassificationStrongFeaturePrecedence)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 真实 shape 正例得到 `agentRoleSubagent/agentReasonSystemAttribution`。
    - 至少覆盖缺前缀、false、相似 key、正文提及、跨 block 拆分、非 text 六类负例。
    - billing header 与 agentContext 既有优先级和测试不回归。
    - feature/classification 结构中不保存 attribution/header/parent/session 原文。
  </acceptance_criteria>
  <done>2.1.207 原生 subagent attribution 被高置信识别，普通 system 文本不能伪造旁路。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将 agent role 贯穿 requestMeta 并锁定唯一状态写回 predicate</name>
  <read_first>
    - internal/proxy/request_meta.go
    - internal/proxy/session.go
    - internal/proxy/phase08_integration_test.go
    - internal/proxy/proxy_pipeline_test.go
    - internal/proxy/forward.go
    - .planning/phases/10-token/10-CONTEXT.md
  </read_first>
  <files>internal/proxy/request_meta.go, internal/proxy/phase08_integration_test.go</files>
  <behavior>
    - D-10/D-11：requestMeta 的 `AgentRole=subagent` 使 `tracksSawtoothState()` 为 false，JSON/SSE 后续复用同一 predicate。
    - nil、零值、main、unknown meta 均默认 true，避免不确定请求失去主会话保护。
    - title 与 subagent 都可写自身 response_usage facts，但不更新 actual/message baseline、request sequence、pause、Frozen invalidation 或 pressure trigger。
    - integration matrix 包含 HTTP billing、agentContext、2.1.207 system attribution 与 unknown-is-main 四行。
  </behavior>
  <action>先扩展 `TestPhase08AgentIsolationMatrix` 和 requestMeta predicate 断言，再为 `requestMeta` 增加 `AgentRole agentRole` 字段；更新 `tracksSawtoothState` 为“nil/零值默认 true，session_title 或 subagent false”。本计划只建立 contract 和矩阵，不修改 `proxy.go`/`forward.go` wiring；10-04 必须在入口把 `classification.Role` 写入 meta 并让 JSON/SSE 使用该 predicate。integration fixture 继续使用 httptest/t.TempDir/脱敏标记，不读取生产 SQLite 或真实 parent/session ID。</action>
  <verify>
    <automated>go test ./internal/proxy -run 'Test(Phase08AgentIsolationMatrix|ForwardSawtoothStatePolicy)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `requestMeta{AgentRole: agentRoleSubagent}` 明确不跟踪 Sawtooth；main/unknown/nil/zero 明确跟踪。
    - isolation matrix 新增 system attribution 行并保持 unknown-is-main 控制行。
    - 测试不引入第三种无状态类型或 parent session 状态键。
    - 仅修改 request-scoped role contract；实际 response wiring 留给有显式依赖的 10-04。
  </acceptance_criteria>
  <done>可靠 subagent 身份具备从请求入口贯穿响应写回的单一权限表达。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| system text blocks → agent role | 不可信 prompt 文本不能仅凭关键词获得 subagent 旁路 |
| HTTP/body classification → requestMeta | 高置信 role 必须完整贯穿，不能在响应路径丢失 |
| requestMeta → Sawtooth state | 单一 predicate 决定 actual/pause/requestSeq 等主状态权限 |
| classification facts → logs/debug | attribution/header/parent/session 原文不得泄漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-03-01 | Spoofing | inspectAgentSystem | high | mitigate | 独立 block + 固定前缀 + token-boundary marker；强/弱形态表驱动测试 |
| T-10-03-02 | Elevation of Privilege | classifyAgentFeatures | high | mitigate | 仅三类已证明强特征授予 subagent；未匹配/畸形保持 main |
| T-10-03-03 | Tampering | requestMeta role propagation | high | mitigate | request-scoped `AgentRole` 与唯一 `tracksSawtoothState` predicate，10-04 端到端验证 |
| T-10-03-04 | Information Disclosure | agent features/logging | high | mitigate | 只存 bool/enum；secret sentinel 与 request_id-only auxiliary logger |
| T-10-03-05 | Denial of Service | system block scan | low | accept | 单次 O(n) block 遍历，受现有 10MB body 上限约束 |
</threat_model>

## Artifacts this phase produces

- 新字段：`agentRequestFeatures.SystemAttributionSubagent`、`requestMeta.AgentRole`。
- 新枚举值：`agentReasonSystemAttribution = "system_attribution"`。
- 修改方法：`inspectAgentSystem` 识别独立 attribution block；`requestMeta.tracksSawtoothState` 同时排除 title/subagent。
- 新测试：`TestAgentSystemAttributionStrongFeature`；扩展 `TestPhase08AgentIsolationMatrix` 与 `TestForwardSawtoothStatePolicy`。
- 新文件：无。

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | 可靠 subagent 不污染主 pressure | 10-03/10-04 | COVERED | role contract + response wiring |
| REQ | PRESSURE-04 | subagent 与其他明确辅助请求状态隔离 | 10-03/10-04 | COVERED | 强分类、meta predicate、JSON/SSE 测试 |
| RESEARCH | — | 2.1.207 attribution block 窄识别 | 10-03 | COVERED | Task 1 |
| CONTEXT | D-10/D-11 | 身份贯穿入口到 usage 写回 | 10-03/10-04 | COVERED | contract + wiring |
| CONTEXT | D-12 | 2.1.207 唯一权威基线 | 10-03 | COVERED | 无版本矩阵 |
| CONTEXT | D-15/D-16 | 独立 attribution block、禁止全文泛搜 | 10-03 | COVERED | Task 1 |
| CONTEXT | D-17 | 不实现冲突/第三模式/未来猜测 | 10-03 | COVERED | main fallback |
| CONTEXT | D-23/D-24 | 脱敏 facts、辅助 usage 但无主摘要 | 10-03/10-04 | COVERED | enum/bool + 后续 observability |

Deferred auxiliary 类型与 Phase 11 状态机未进入本计划。

<verification>
运行分类、requestMeta 与 Phase 08 isolation 定向测试；10-04 完成入口/响应 wiring 后运行全包测试。
</verification>

<success_criteria>
- 2.1.207 system attribution 正例稳定命中，关键词相似负例全部 fail closed。
- billing header、agentContext 与 unknown-is-main 既有行为不回归。
- title/subagent 共享唯一状态 predicate，nil/zero/main/unknown 默认安全跟踪。
- 分类状态与日志不包含 attribution/header/parent/session 原文。
</success_criteria>

<output>
Create `.planning/phases/10-token/10-03-SUMMARY.md` when done
</output>
