---
phase: 10-token
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/proxy/auxiliary.go
  - internal/proxy/auxiliary_test.go
autonomous: true
requirements:
  - PRESSURE-04
must_haves:
  truths:
    - "D-12/D-13：Claude Code 2.1.207 session title 仍要求单条 user、完整平衡 session envelope、严格 title system intent 与 title-only schema或 output_config 完全缺失回退"
    - "D-14：envelope 后只接受 2.1.207 源码证明的语言指令；任意尾随文本、额外 envelope、null/畸形/非 title-only output_config 均 fail closed"
    - "D-10/D-24：高置信 title 分类为只读 auxiliary，后续可保留 response_usage facts，但不得获得主会话 pressure 状态权限或主压力摘要"
    - "D-17：不新增 rename、compact、prompt suggestion、memory extraction、teleport 或未来协议框架"
  artifacts:
    - path: "internal/proxy/auxiliary.go"
      provides: "2.1.207 session envelope + 受控语言后缀的严格 title 分类"
    - path: "internal/proxy/auxiliary_test.go"
      provides: "受控后缀正例、相似文本负例与 output_config fail-closed 矩阵"
  key_links:
    - from: "classifyAuxiliaryRequest"
      to: "splitSessionTitleEnvelope"
      via: "先验证单一完整 envelope，再验证精确受控语言后缀"
      pattern: "classifyAuxiliaryRequest|splitSessionTitleEnvelope"
    - from: "inspectAuxiliaryOutputConfig"
      to: "requestKindSessionTitle"
      via: "仅 missing 或严格 title-only schema 允许分类"
      pattern: "auxiliaryOutputConfig(Missing|TitleOnly|Invalid)"
---

<objective>
把 Claude Code 2.1.207 已证明的 session title 语言后缀纳入严格分类器，同时保持所有相似或畸形请求按主请求处理。

Purpose: 防止真实 2.1.207 title 请求漏识别后污染 pressure/Frozen/Archive 状态，也防止宽松尾缀匹配把普通主请求错误旁路。
Output: 受控 envelope 后缀解析 helper 与表驱动强/弱路径回归。
</objective>

<execution_context>
@C:/Users/romantcig/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/romantcig/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-token/10-CONTEXT.md
@.planning/phases/10-token/10-RESEARCH.md
@.planning/phases/10-token/10-PATTERNS.md

<interfaces>
- 2.1.207 权威构造是 `<session>...\n</session>\n\n{languageInstruction}`。
- 默认语言指令精确为：`Write the title in the predominant language of the session — a stray word or code token in another language doesn't change it. Ignore the language of the examples above.`
- 配置语言指令形态精确为：`Write the title in {language}. Keep technical terms and code identifiers in their original form.`；`{language}` 必须是单行、非空、受限长度的可见语言名，不能包含标签、控制符或第二条指令。
- 当前 `hasCompleteSessionEnvelope` 的 32 层平衡、单顶层和 fail-closed 语义必须保留。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 解析单一 session envelope 与 2.1.207 受控语言后缀</name>
  <read_first>
    - internal/proxy/auxiliary.go
    - internal/proxy/auxiliary_test.go
    - .planning/phases/10-token/10-CONTEXT.md
    - .planning/phases/10-token/10-RESEARCH.md
  </read_first>
  <files>internal/proxy/auxiliary.go, internal/proxy/auxiliary_test.go</files>
  <behavior>
    - D-13：无后缀的单一完整 envelope 继续通过；平衡嵌套最多 32 层继续通过。
    - D-14：默认 predominant-language 句子和受限 `Write the title in {language}...` 句子通过。
    - D-14：任意额外说明、第二个句子、额外顶层 envelope、提前闭合、未闭合、控制字符或伪语言标签拒绝。
    - D-12：测试只面向 2.1.207 形态，不创建 2.1.199 分支或版本矩阵。
  </behavior>
  <action>先扩展 `TestClassifyAuxiliaryRequest` RED 表，再将 `hasCompleteSessionEnvelope` 重构为 `splitSessionTitleEnvelope(text string) (suffix string, ok bool)` 或等价纯函数：线性扫描并验证唯一顶层 `<session>`、最大深度 32，闭合后只允许空白或一个精确语言指令。新增 `isSessionTitleLanguageInstruction`，接受默认完整句和配置语言模板；模板中的语言名限制为去首尾空白后 1–64 个 Unicode 字符、单行、无 `<`/`>`、无控制字符，且必须以固定前后缀完整匹配。不得用 `strings.Contains` 泛化后缀，不得解释 envelope 内正文。</action>
  <verify>
    <automated>go test ./internal/proxy -run 'TestClassifyAuxiliaryRequest' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 现有无后缀、嵌套与 32 层边界测试继续通过。
    - 两种 2.1.207 语言指令正例分类为 `session_title`。
    - 至少覆盖尾随任意文本、第二指令、控制符、伪标签、超长语言名五类负例。
    - helper 只检查结构与固定语句，不读取或记录 session 正文。
  </acceptance_criteria>
  <done>真实 2.1.207 title envelope 被严格识别，普通尾随文本无法借此获得 auxiliary 身份。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁定 title schema、prompt fallback 与非目标 auxiliary 排除边界</name>
  <read_first>
    - internal/proxy/auxiliary.go
    - internal/proxy/auxiliary_test.go
    - internal/proxy/proxy_pipeline_test.go
    - .planning/phases/10-token/10-CONTEXT.md
  </read_first>
  <files>internal/proxy/auxiliary.go, internal/proxy/auxiliary_test.go</files>
  <behavior>
    - D-13：`output_config` 完全缺失时只在其余 title 强门禁全部满足后使用 `title_prompt_fallback`。
    - D-14：`output_config` present-but-null、畸形、非 json_schema、额外 property、非 string title 或 additionalProperties=true 一律拒绝。
    - D-17：rename、teleport、compact、prompt suggestion、memory extraction 继续拒绝，即使带合法 session envelope 或 title-only schema。
    - D-23：classification log 只保留 request kind/reason/message count/request_id，不包含 title/system/session 正文。
  </behavior>
  <action>在现有表驱动测试中为带语言后缀的 schema/missing-output_config 各加正例，并为 null/畸形/非 title-only 与非目标 auxiliary 加交叉负例。保持 `inspectAuxiliaryOutputConfig` 三态设计和 `isSessionTitleSystem` 严格意图组合；若实现重构，只允许将重复结构拆成纯 helper，不得放宽 system intent、tools/thinking/model 不参与硬门禁的既有决定。扩展 `TestAuxiliaryClassificationLog` sentinel，证明语言名、session 正文和 system prompt 不进入日志。</action>
  <verify>
    <automated>go test ./internal/proxy -run 'Test(ClassifyAuxiliaryRequest|AuxiliaryClassificationLog|SessionTitleFixture)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 带 2.1.207 后缀的 title-only schema 与 output_config-missing 两条强路径通过。
    - present-but-invalid 的每种 output_config 仍 fail closed。
    - 五类 deferred auxiliary 没有新增 kind/reason 或旁路。
    - 日志 sentinel 扫描不包含正文、语言名、session ID、header 或凭证。
  </acceptance_criteria>
  <done>title 分类只扩展到已证明的 2.1.207 wire，不扩大辅助请求产品范围或敏感日志面。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user message text → session title classifier | 不可信正文可伪造标签或尾随指令，必须结构化 fail closed |
| system/output_config → auxiliary identity | 只有严格意图与 title-only schema 可授予无状态旁路 |
| classification → audit log | title/session/system 正文不得进入日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-02-01 | Spoofing | splitSessionTitleEnvelope | high | mitigate | 单顶层、平衡深度、完整消费输入、固定后缀正/负矩阵 |
| T-10-02-02 | Elevation of Privilege | classifyAuxiliaryRequest | high | mitigate | system intent + envelope + schema 三重门禁；任一异常按主请求处理 |
| T-10-02-03 | Tampering | output_config parser | high | mitigate | present-but-invalid 一律拒绝，禁止 prompt fallback 掩盖畸形配置 |
| T-10-02-04 | Information Disclosure | auxiliary audit logger | high | mitigate | request_id-only logger 与敏感 sentinel 扫描，不记录正文/语言/session/header |
| T-10-02-05 | Denial of Service | nested session scanner | low | accept | O(n) 扫描且最大深度 32，沿用既有请求体上限 |
</threat_model>

## Artifacts this phase produces

- 新 helper：`splitSessionTitleEnvelope`、`isSessionTitleLanguageInstruction`。
- 修改行为：`classifyAuxiliaryRequest` 接受 2.1.207 的两个受控语言指令形态。
- 新测试用例（并入既有函数）：2.1.207 默认语言正例、配置语言正例、任意尾缀/第二指令/控制符/伪标签/超长语言名负例、schema 与 missing-output_config 交叉矩阵。
- 新顶层测试函数：无；扩展 `TestClassifyAuxiliaryRequest`、`TestAuxiliaryClassificationLog`、`TestSessionTitleFixture`。
- 新文件：无。

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | title 辅助请求不污染主 pressure | 10-02/10-04 | COVERED | 本计划保证识别，10-04 保证状态只读 |
| REQ | PRESSURE-04 | session title 不读取/推进/覆盖主状态 | 10-02/10-04 | COVERED | 高置信分类 + response gate |
| RESEARCH | — | 2.1.207 envelope 后缀窄匹配 | 10-02 | COVERED | Tasks 1-2 |
| CONTEXT | D-10 | title 只读 auxiliary | 10-02/10-04 | COVERED | 分类与 wiring 分工 |
| CONTEXT | D-12～D-14 | 2.1.207 唯一基线与严格 title 形态 | 10-02 | COVERED | Tasks 1-2 |
| CONTEXT | D-17 | 不实现未来 auxiliary 框架 | 10-02 | COVERED | 负例与范围 fence |
| CONTEXT | D-23/D-24 | 脱敏 usage、无主压力摘要 | 10-02/10-04 | COVERED | 日志门禁 + 后续 facts wiring |

Deferred Ideas 保持排除，没有新增 auxiliary kind。

<verification>
运行 title 分类和日志定向测试；10-04 再通过主管线 JSON/SSE 集成测试证明 pressure 状态只读。
</verification>

<success_criteria>
- 2.1.207 真实 title 后缀稳定命中。
- 任意尾随文本和畸形 schema 不会获得 auxiliary 身份。
- 非目标 auxiliary 继续按主请求处理，不新增版本兼容矩阵。
- classification audit 不泄漏 title/session/system/header/凭证正文。
</success_criteria>

<output>
Create `.planning/phases/10-token/10-02-SUMMARY.md` when done
</output>
