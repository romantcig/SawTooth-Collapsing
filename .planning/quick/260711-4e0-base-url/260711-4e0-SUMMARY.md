---
quick_id: 260711-4e0
status: complete
commit: 1fa804d
---

# 修复上游 Base URL 尾斜杠导致请求路径错误 - 摘要

## 完成内容

- 在上游请求 URL 拼接前移除 `proxy.target` 末尾的斜杠，避免生成 `//v1/messages`。
- 添加真实 HTTP 转发回归测试，覆盖路径和查询参数的保留。

## 验证

- 修改前新增测试失败，实际捕获到 `//v1/messages?beta=true`。
- 修改后 `go test ./internal/proxy -run '^TestForwardRawTargetTrailingSlash$' -count=1` 通过。
- `go test ./...` 通过。
- `go build -o /tmp/sawtooth-proxy-test.exe ./cmd/proxy` 通过。

