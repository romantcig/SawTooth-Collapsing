---
quick_id: 260711-4e0
status: complete
---

# 修复上游 Base URL 尾斜杠导致请求路径错误

## 目标

确保 `proxy.target` 无论是否带尾斜杠，`POST /v1/messages` 都转发到相同且正确的上游路径。

## 任务

1. 为带尾斜杠的上游地址添加真实 HTTP 转发回归测试。
   - 验证：本地测试上游收到 `/v1/messages`，而不是 `//v1/messages`。
2. 在现有上游 URL 拼接点做最小规范化处理。
   - 验证：新增测试通过，原有行为保持不变。
3. 运行相关测试与全量 Go 测试。
   - 验证：`go test ./...` 通过。
